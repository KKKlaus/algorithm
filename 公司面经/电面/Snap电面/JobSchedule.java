public class JobSchedule {

    //给一组List<Job>, 其中job class中含有该job的name 和该job的parent Job, class Job {
    //String name;
    //String parentJob
    //}
    //
    //要求输出所有的root job(没有parent job的job)和他的leaf jobs


    // 方法：先遍历建graph map + indegree array 然后bfs
}
